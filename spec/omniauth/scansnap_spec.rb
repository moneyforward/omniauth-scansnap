require 'spec_helper'

describe Omniauth::Scansnap do
  it 'has a version number' do
    expect(Omniauth::Scansnap::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
